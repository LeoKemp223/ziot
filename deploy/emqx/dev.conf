# Development callback endpoints used by the local Docker Compose network.
# EMQX can reach the Next.js app by the Compose service name `web`.

auth_url = "http://web:3000/api/internal/emqx/auth"
acl_url = "http://web:3000/api/internal/emqx/acl"
webhook_url = "http://web:3000/api/internal/emqx/webhook"
